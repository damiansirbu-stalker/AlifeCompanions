--- _ac_manifest: AlifeCompanions identity. Pure data, no logic. Read by _ac_init (the gate) and the validator (deps.manifest_structure).

MANIFEST = {
    name    = "AlifeCompanions",
    version = "next",
    xlibs   = "1.5.1",
}
